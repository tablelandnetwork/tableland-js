{
  "scripts": {
    "test": "node --experimental-fetch node_modules/.bin/jest --verbose",
    "up": "HARDHAT_DIR=../eth-tableland VALIDATOR_DIR=../go-tableland/local deno run --allow-run --allow-env up.ts"
  },
  "type": "module",
  "devDependencies": {
    "@tableland/sdk": "^0.0.2",
    "@types/jest": "^27.5.1",
    "ethers": "^5.6.6",
    "jest": "^28.1.0",
    "node-fetch": "^3.2.4",
    "ts-jest": "^28.0.2"
  }
}
